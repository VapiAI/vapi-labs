# Deprecated intentionally: do not put setup credentials or Worker secrets in .env.
# Wrangler may infer .env values as Worker bindings, including during remote preview.
# Use .setup.env.example for administrative setup credentials and .dev.vars for the
# local Worker's ORDERS_API_TOKEN.
